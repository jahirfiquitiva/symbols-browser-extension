# Build output. Symbols SVGs are copied here from node_modules at build time and
# are deliberately never committed to this repository.
dist/
*.zip
src/generated/

# Safari Xcode project, produced by `npm run bundle-safari`
safari/

# The Symbols icon set, fetched by scripts/fetch-icons.ts from the release
# pinned in symbols.json. Deliberately never committed.
vendor/

node_modules/

# Reference repositories, kept locally for comparison only
reference/

*.log
.DS_Store
.env
.idea
Thumbs.db
